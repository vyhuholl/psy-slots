## 1. Профиль клиента (данные)

- [ ] 1.1 Тест: `Client` валидирует IANA-таймзону; невалидная → ошибка (падающий)
- [ ] 1.2 Тест: `client_service.set_timezone` сохраняет, `get` возвращает; неизвестный клиент → без TZ
- [ ] 1.3 Реализовать `app/domain/client.py` (модель + валидация TZ) и `app/services/client_service.py` (`get`, `set_timezone`)
- [ ] 1.4 Тест: раннер миграций создаёт `clients`; идемпотентно
- [ ] 1.5 Добавить `CREATE TABLE IF NOT EXISTS clients (telegram_id, timezone, created_at)` в `app/migrations.py`
- [ ] 1.6 Зелёные тесты 1.1–1.4

## 2. callback_data и клавиатуры

- [ ] 2.1 Тест: (де)сериализация `slot:` / `confirm:` / `cancel:` / `tz:`; round-trip; длина ≤ 64 байт для реального epoch/UUID (падающий)
- [ ] 2.2 Тест: сборка клавиатур — слоты на сегодня, свои брони, выбор таймзоны
- [ ] 2.3 Реализовать `app/bot/keyboards.py`: единая (де)сериализация `callback_data`, билдеры клавиатур
- [ ] 2.4 Зелёные тесты 2.1–2.2

## 3. Форматирование времени в TZ клиента

- [ ] 3.1 Тест: UTC → строка в TZ клиента (слот, подтверждение); не UTC (падающий)
- [ ] 3.2 Реализовать `app/bot/formatting.py`: форматирование через `zoneinfo`
- [ ] 3.3 Зелёный тест 3.1

## 4. Хендлеры потока записи

- [ ] 4.1 Тест: `/start` показывает приветствие из конфигурации (падающий, сервисы замоканы)
- [ ] 4.2 Тест: `/book` без TZ клиента → запрос таймзоны; `tz:<name>` сохраняет таймзону и продолжает
- [ ] 4.3 Тест: `/book` с TZ и наличием слотов → список сегодняшних свободных слотов из `slot_service` (во времени клиента)
- [ ] 4.4 Тест: `/book` без свободных слотов на сегодня → сообщение о невозможности брони из конфигурации
- [ ] 4.5 Тест: `confirm:` → `booking_service.create`, подтверждение клиенту
- [ ] 4.6 Тест: `confirm:` при `SlotTaken` → дружелюбное «слот занят», без исключения и без второй брони
- [ ] 4.7 Тест: повторный `confirm:` → вторая бронь не создаётся, показан согласованный результат
- [ ] 4.8 Реализовать `app/bot/handlers/client.py`: тонкие хендлеры `/start`, `/book`, `tz:`, `slot:`, `confirm:`; сегодняшнее окно → UTC → `slot_service`; ветка «нет слотов»; обработка ошибок
- [ ] 4.9 Зелёные тесты 4.1–4.7

## 5. Просмотр и отмена своих броней

- [ ] 5.1 Тест: `/mybookings` → список активных броней клиента во времени клиента; пусто → сообщение без ошибки (падающий)
- [ ] 5.2 Тест: `cancel:<id>` (с confirm-шагом) → `booking_service.cancel`, подтверждение отмены
- [ ] 5.3 Реализовать хендлеры `/mybookings` и отмены в `handlers/client.py`
- [ ] 5.4 Зелёные тесты 5.1–5.2

## 6. Подключение и завершение

- [ ] 6.1 Подключить клиентские роутеры к Dispatcher из `bootstrap-bot`
- [ ] 6.2 `make test` зелёный, покрытие ≥ 80%
- [ ] 6.3 `make validate` зелёный (ruff + format + mypy, без `# type: ignore`)
