## 1. Привязка специалиста к Telegram (дельта registry)

- [ ] 1.1 Тест: `telegram_id` на записи специалиста; `get_by_telegram_id` возвращает специалиста; непривязанный → «не найдено» (падающий)
- [ ] 1.2 Тест: повторная привязка того же `telegram_id` к другому специалисту → доменная ошибка
- [ ] 1.3 Реализовать: поле `telegram_id` в `app/domain/specialist.py`, `set_telegram_id`/`get_by_telegram_id` в `specialist_service`
- [ ] 1.4 Тест: миграция добавляет колонку `telegram_id` + индекс; идемпотентно
- [ ] 1.5 Добавить идемпотентный `ALTER TABLE specialists ADD COLUMN telegram_id` + индекс в `app/migrations.py`
- [ ] 1.6 Зелёные тесты 1.1–1.4

## 2. Гвард инвариантности (домен)

- [ ] 2.1 Тест: изменение длительности при наличии активных будущих броней → блокировка, длительность не меняется (падающий, брони замоканы)
- [ ] 2.2 Тест: изменение длительности без активных будущих броней → успех
- [ ] 2.3 Тест: удаление интервала с активной бронью внутри будущих вхождений → блокировка
- [ ] 2.4 Тест: удаление интервала без броней внутри → успех
- [ ] 2.5 Реализовать `app/services/schedule_guard.py`: запрос активных будущих броней, фильтр по интервалу (weekday+время), политика блокировки
- [ ] 2.6 Зелёные тесты 2.1–2.4

## 3. Авторизация

- [ ] 3.1 Тест: привязанный специалист → доступ к админ-меню; непривязанный → отказ (падающий, сервис замокан)
- [ ] 3.2 Реализовать проверку авторизации (middleware/фильтр по `get_by_telegram_id`) для админ-роутера
- [ ] 3.3 Зелёный тест 3.1

## 4. Хендлеры: длительность и расписание

- [ ] 4.1 Тест: изменение длительности — успех и блокировка (гвард) с понятным сообщением (падающий, моки)
- [ ] 4.2 Тест: добавление интервала → сервис; удаление свободного интервала → сервис; удаление с бронями → блок
- [ ] 4.3 Реализовать в `app/bot/handlers/admin.py`: тонкие хендлеры длительности и расписания через сервисы + гвард; клавиатуры/`callback_data` в `keyboards.py`
- [ ] 4.4 Зелёные тесты 4.1–4.2

## 5. Хендлеры: просмотр и отмена броней

- [ ] 5.1 Тест: список активных броней специалиста во времени специалиста; пусто → сообщение без ошибки (падающий)
- [ ] 5.2 Тест: отмена брони → `booking_service.cancel` + уведомление клиента прямым сообщением
- [ ] 5.3 Тест: отмена уже отменённой → без ошибки и без повторного эффекта (идемпотентно)
- [ ] 5.4 Реализовать хендлеры просмотра/отмены в `handlers/admin.py`, уведомление клиента (`bot.send_message` в TZ клиента)
- [ ] 5.5 Зелёные тесты 5.1–5.3

## 6. Подключение и завершение

- [ ] 6.1 Подключить админ-роутеры к Dispatcher из `bootstrap-bot`
- [ ] 6.2 `make test` зелёный, покрытие ≥ 80%
- [ ] 6.3 `make validate` зелёный (ruff + format + mypy, без `# type: ignore`)
- [ ] 6.4 `openspec validate add-specialist-admin-flow --strict` проходит
