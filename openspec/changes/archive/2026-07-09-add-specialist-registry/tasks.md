## 1. Доменная модель специалиста

- [x] 1.1 Тест: `Specialist` создаётся с валидными полями (id, имя, длительность, IANA-таймзона) и неизменяем (падающий)
- [x] 1.2 Тест: длительность ≤ 0 → доменная ошибка валидации
- [x] 1.3 Тест: некорректный IANA-идентификатор таймзоны → доменная ошибка валидации
- [x] 1.4 Реализовать `app/domain/specialist.py`: frozen `Specialist`, валидация длительности (> 0) и таймзоны (`zoneinfo.ZoneInfo`), доменные ошибки (`ValidationError`, `SpecialistNotFound`)
- [x] 1.5 Зелёные тесты 1.1–1.3

## 2. Таблица specialists

- [x] 2.1 Тест: раннер миграций создаёт таблицу `specialists`; повторный прогон идемпотентен (падающий, сессия YDB замокана)
- [x] 2.2 Добавить `CREATE TABLE IF NOT EXISTS specialists (id, name, slot_duration_minutes, timezone, created_at)` в `app/migrations.py`
- [x] 2.3 Зелёный тест 2.1

## 3. Доменный сервис

- [x] 3.1 Тест: `register(...)` создаёт строку с сгенерированным UUID; `get(id)` возвращает сохранённые значения (падающий, пул YDB замокан)
- [x] 3.2 Тест: `list()` возвращает всех специалистов при 0/1/N записях, без опоры на захардкоженное число
- [x] 3.3 Тест: `get(unknown_id)` → явный результат «не найдено» без утечки исключения хранилища
- [x] 3.4 Тест: `update_duration` / `update_timezone` существующего специалиста сохраняют новое значение
- [x] 3.5 Тест: `update_duration` / `update_timezone` несуществующего id → `SpecialistNotFound`, новая строка не создаётся
- [x] 3.6 Тест: `update_duration` с невалидным значением и `update_timezone` с невалидной зоной → ошибка валидации, значение не сохраняется
- [x] 3.7 Реализовать `app/services/specialist_service.py`: `register`, `list`, `get`, `update_duration`, `update_timezone` над пулом YDB; маппинг строка⇄модель; генерация `uuid4`
- [x] 3.8 Зелёные тесты 3.1–3.6

## 4. Проверка и завершение

- [x] 4.1 `make test` зелёный, покрытие ≥ 80%
- [x] 4.2 `make validate` зелёный (ruff + format + mypy, без `# type: ignore`)