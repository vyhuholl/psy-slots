## 1. Чистое ядро: сетка слотов

- [x] 1.1 Тест: интервал 10:00–11:00, D=15 → слоты 10:00/10:15/10:30/10:45 (падающий)
- [x] 1.2 Тест: интервал 10:00–10:50, D=15 → без хвостового 10:45–11:00
- [x] 1.3 Тест: `Slot` неизменяем; выдача упорядочена по `start`
- [x] 1.4 Реализовать `app/domain/slots.py`: frozen `Slot`, полуоткрытая сетка `t+D ≤ end`, без персистентности
- [x] 1.5 Зелёные тесты 1.1–1.3

## 2. Чистое ядро: материализация в UTC и DST

- [x] 2.1 Тест: слот даты переводится в UTC по TZ специалиста; совпадение дня недели (падающий)
- [x] 2.2 Тест: несуществующее локальное время (весенний переход) → слот пропущен
- [x] 2.3 Тест: неоднозначное локальное время (осенний переход) → ровно один слот (`fold=0`), без дубля
- [x] 2.4 Реализовать материализацию в `slots.py`: `ZoneInfo`, round-trip проверка существования (skip), `fold=0` для неоднозначного, перебор дат окна с запасом ±1 день
- [x] 2.5 Зелёные тесты 2.1–2.3

## 3. Сервис: исключение занятых и окно

- [x] 3.1 Тест: слот, пересекающий активную бронь, исключён из выдачи (падающий, репозитории замоканы)
- [x] 3.2 Тест: слот отменённой брони снова свободен (присутствует в выдаче)
- [x] 3.3 Тест: фильтр по окну `[from, to)` — только `from ≤ start < to`, упорядочено
- [x] 3.4 Тест: пустая выдача при отсутствии доступности или дат в окне
- [x] 3.5 Реализовать `app/services/slot_service.py`: подтянуть специалиста (D+TZ), интервалы, активные брони; вызвать ядро; вычесть пересечения `[start,end)`; фильтр по окну
- [x] 3.6 Зелёные тесты 3.1–3.4

## 4. Проверка и завершение

- [x] 4.1 `make test` зелёный, покрытие ≥ 80%
- [x] 4.2 `make validate` зелёный (ruff + format + mypy, без `# type: ignore`)