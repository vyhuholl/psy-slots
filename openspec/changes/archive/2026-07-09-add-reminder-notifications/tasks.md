## 1. Таблица sent_reminders

- [x] 1.1 Тест: раннер миграций создаёт `sent_reminders` (PK `booking_id`); идемпотентно (падающий)
- [x] 1.2 Добавить `CREATE TABLE IF NOT EXISTS sent_reminders (booking_id, sent_at)` в `app/migrations.py`
- [x] 1.3 Зелёный тест 1.1

## 2. Сервис напоминаний

- [x] 2.1 Тест: бронь созрела (`now ≥ start − 5 мин`, бронь будущая) → выбрана; больше 5 минут до начала → нет (падающий, брони/маркеры замоканы)
- [x] 2.2 Тест: идемпотентность — при существующем маркере `booking_id` повторной отправки нет
- [x] 2.3 Тест: отменённая и прошедшая бронь исключены
- [x] 2.4 Тест: текст форматируется в TZ клиента (не UTC)
- [x] 2.5 Тест: ошибка отправки одного получателя не роняет проход; маркер не фиксируется как отправленный (claim снимается)
- [x] 2.6 Тест: лид-тайм — фиксированная константа 5 минут, не из окружения
- [x] 2.7 Реализовать `app/services/reminder_service.py`: константа `REMINDER_LEAD` (5 минут), выборка кандидатов в горизонте 5 минут, проверка зрелости, claim-then-send маркера по `booking_id`, отправка через `Bot` + `formatting`, устойчивость к ошибкам
- [x] 2.8 Зелёные тесты 2.1–2.6

## 3. Точка входа timer-функции

- [x] 3.1 Тест: вызов `notify` обрабатывает созревшие напоминания и завершается без фоновых задач (падающий, сервис замокан)
- [x] 3.2 Реализовать `app/notify.py`: entrypoint timer-триггера, тёплый `Bot`, один проход через `reminder_service`, `asyncio.run` для отправки; без planner/фоновых задач
- [x] 3.3 Зелёный тест 3.1

## 4. Проверка и завершение

- [x] 4.1 `make test` зелёный, покрытие ≥ 80%
- [x] 4.2 `make validate` зелёный (ruff + format + mypy, без `# type: ignore`)
