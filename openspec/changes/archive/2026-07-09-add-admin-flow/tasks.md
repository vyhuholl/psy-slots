## 1. Авторизация

- [x] 1.1 Тест: пользователь с id = `ADMIN_TELEGRAM_ID` → доступ к админ-меню; прочий → отказ (падающий, конфиг замокан)
- [x] 1.2 Реализовать проверку авторизации (middleware/фильтр по `ADMIN_TELEGRAM_ID`) для админ-роутера
- [x] 1.3 Зелёный тест 1.1

## 2. Просмотр броней

- [x] 2.1 Тест: список активных броней во времени конфигурации; пусто → сообщение без ошибки (падающий, сервис замокан)
- [x] 2.2 Реализовать хендлер просмотра в `app/bot/handlers/admin.py` (через `booking_service` read + форматирование в TZ конфигурации); клавиатуры/`callback_data` в `keyboards.py`
- [x] 2.3 Зелёный тест 2.1

## 3. Отмена брони

- [x] 3.1 Тест: отмена брони → `booking_service.cancel` + уведомление клиента прямым сообщением (падающий)
- [x] 3.2 Тест: отмена уже отменённой → без ошибки и без повторного эффекта (идемпотентно)
- [x] 3.3 Реализовать хендлер отмены в `handlers/admin.py`, уведомление клиента (`bot.send_message` в TZ клиента)
- [x] 3.4 Зелёные тесты 3.1–3.2

## 4. Подключение и завершение

- [x] 4.1 Подключить админ-роутеры к Dispatcher из `bootstrap-bot`
- [x] 4.2 `make test` зелёный, покрытие ≥ 80%
- [x] 4.3 `make validate` зелёный (ruff + format + mypy, без `# type: ignore`)
