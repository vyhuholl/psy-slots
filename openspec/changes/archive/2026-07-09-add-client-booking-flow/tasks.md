## 1. Профиль клиента (данные)

- [x] 1.1 Тест: `Client` валидирует IANA-таймзону; невалидная → ошибка (падающий)
- [x] 1.2 Тест: `client_service.set_timezone` сохраняет, `get` возвращает; неизвестный клиент → без TZ
- [x] 1.3 Реализовать `app/domain/client.py` (модель + валидация TZ) и `app/services/client_service.py` (`get`, `set_timezone`)
- [x] 1.4 Тест: раннер миграций создаёт `clients`; идемпотентно
- [x] 1.5 Добавить `CREATE TABLE IF NOT EXISTS clients (telegram_id, timezone, created_at)` в `app/migrations.py`
- [x] 1.6 Зелёные тесты 1.1–1.4

## 2. callback_data и клавиатуры

- [x] 2.1 Тест: (де)сериализация `slot:` / `confirm:` / `cancel:` / `tz:`; round-trip; длина ≤ 64 байт для реального epoch/UUID (падающий)
- [x] 2.2 Тест: сборка клавиатур — слоты на сегодня, свои брони, выбор таймзоны
- [x] 2.3 Реализовать `app/bot/keyboards.py`: единая (де)сериализация `callback_data`, билдеры клавиатур
- [x] 2.4 Зелёные тесты 2.1–2.2

## 3. Форматирование времени в TZ клиента

- [x] 3.1 Тест: UTC → строка в TZ клиента (слот, подтверждение); не UTC (падающий)
- [x] 3.2 Реализовать `app/bot/formatting.py`: форматирование через `zoneinfo`
- [x] 3.3 Зелёный тест 3.1

## 4. Хендлеры потока записи

- [x] 4.1 Тест: `/start` показывает приветствие из конфигурации (падающий, сервисы замоканы)
- [x] 4.2 Тест: `/book` без TZ клиента → запрос таймзоны; `tz:<name>` сохраняет таймзону и продолжает
- [x] 4.3 Тест: `/book` с TZ и наличием слотов → список сегодняшних свободных слотов из `slot_service` (во времени клиента)
- [x] 4.4 Тест: `/book` без свободных слотов на сегодня → сообщение о невозможности брони из конфигурации
- [x] 4.5 Тест: `confirm:` → `booking_service.create`, подтверждение клиенту
- [x] 4.6 Тест: `confirm:` при `SlotTaken` → дружелюбное «слот занят», без исключения и без второй брони
- [x] 4.7 Тест: повторный `confirm:` → вторая бронь не создаётся, показан согласованный результат
- [x] 4.8 Реализовать `app/bot/handlers/client.py`: тонкие хендлеры `/start`, `/book`, `tz:`, `slot:`, `confirm:`; сегодняшнее окно → UTC → `slot_service`; ветка «нет слотов»; обработка ошибок
- [x] 4.9 Зелёные тесты 4.1–4.7

## 5. Просмотр и отмена своих броней

- [x] 5.1 Тест: `/mybookings` → список активных броней клиента во времени клиента; пусто → сообщение без ошибки (падающий)
- [x] 5.2 Тест: `cancel:<id>` (с confirm-шагом) → `booking_service.cancel`, подтверждение отмены
- [x] 5.3 Реализовать хендлеры `/mybookings` и отмены в `handlers/client.py`
- [x] 5.4 Зелёные тесты 5.1–5.2

## 6. Подключение и завершение

- [x] 6.1 Подключить клиентские роутеры к Dispatcher из `bootstrap-bot`
- [x] 6.2 `make test` зелёный, покрытие ≥ 80%
- [x] 6.3 `make validate` зелёный (ruff + format + mypy, без `# type: ignore`)
