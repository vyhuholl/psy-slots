## 1. Доменная модель брони

- [x] 1.1 Тест: `Booking` создаётся с id, client_id, start/end в UTC, состоянием `booked`; неизменяем (падающий)
- [x] 1.2 Тест: `BookingStatus` содержит `booked` и `cancelled`; переход `booked → cancelled` отражается в модели
- [x] 1.3 Реализовать `app/domain/booking.py`: frozen `Booking`, `BookingStatus`, доменные ошибки (`SlotNotToday`, `SlotOutsideAvailability`, `SlotMisaligned`, `SlotTaken`, `BookingNotFound`)
- [x] 1.4 Зелёные тесты 1.1–1.2

## 2. Таблица bookings

- [x] 2.1 Тест: раннер миграций создаёт `bookings` и индекс `(start_utc)`; повторный прогон идемпотентен (падающий, сессия YDB замокана)
- [x] 2.2 Добавить `CREATE TABLE IF NOT EXISTS bookings (id, client_id, start_utc, end_utc, status, created_at, cancelled_at)` + вторичный индекс в `app/migrations.py`
- [x] 2.3 Зелёный тест 2.1

## 3. Создание брони

- [x] 3.1 Тест: создание фиксирует UUID, `start` в UTC и денормализованный `end = start + длительность` из конфигурации (падающий, пул/транзакция и конфиг замоканы)
- [x] 3.2 Тест: изменение длительности в конфигурации после создания не меняет `end` существующей брони
- [x] 3.3 Тест: слот сегодня, внутри окна и на сетке → бронь создаётся
- [x] 3.4 Тест: слот не на сегодня (другой день / прошедшее время) → `SlotNotToday`, не создаётся
- [x] 3.5 Тест: слот вне интервала доступности → `SlotOutsideAvailability`, не создаётся
- [x] 3.6 Тест: слот не кратен длительности или `end` за концом интервала → `SlotMisaligned`, не создаётся
- [x] 3.7 Реализовать `create` в `app/services/booking_service.py`: чтение длительности/TZ/интервалов из конфигурации, денормализация end, конвертация UTC→локаль, проверка «сегодня»/окна/выравнивания, генерация `uuid4`
- [x] 3.8 Зелёные тесты 3.1–3.6

## 4. Запрет двойного бронирования (race-safe)

- [x] 4.1 Тест: пересечение с активной бронью → `SlotTaken`, вторая не создаётся (падающий)
- [x] 4.2 Тест: конкурентное создание одного слота (эмуляция конфликта транзакции) → ровно одна успешна, другая `SlotTaken`
- [x] 4.3 Тест: повторное бронирование слота после отмены предыдущей → успешно (отменённая не блокирует)
- [x] 4.4 Реализовать проверку перекрытия `[start,end)` внутри сериализуемой транзакции YDB (select активных → insert), трактовку конфликта транзакции как `SlotTaken`
- [x] 4.5 Зелёные тесты 4.1–4.3

## 5. Отмена (soft, идемпотентная)

- [x] 5.1 Тест: отмена `booked` → `cancelled`, строка сохраняется и читается по id (падающий)
- [x] 5.2 Тест: повторная отмена уже `cancelled` → успех, состояние прежнее, второго эффекта нет (идемпотентность)
- [x] 5.3 Тест: отмена неизвестного id → `BookingNotFound`
- [x] 5.4 Реализовать `cancel` в `booking_service.py`: soft-переход + `cancelled_at`, идемпотентность, запрет hard-delete
- [x] 5.5 Зелёные тесты 5.1–5.3

## 6. Чтение броней

- [x] 6.1 Тест: `get(id)` возвращает бронь с состоянием; неизвестный id → «не найдено» (падающий)
- [x] 6.2 Тест: `list_active_in_range(range)` возвращает только `booked` в диапазоне, исключает отменённые
- [x] 6.3 Тест: `list_active_for_client(client_id)` возвращает только его `booked`
- [x] 6.4 Реализовать read-операции в `booking_service.py`
- [x] 6.5 Зелёные тесты 6.1–6.3

## 7. Проверка и завершение

- [x] 7.1 `make test` зелёный, покрытие ≥ 80%
- [x] 7.2 `make validate` зелёный (ruff + format + mypy, без `# type: ignore`)
