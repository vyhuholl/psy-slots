## 1. Конфигурация смещений

- [ ] 1.1 Тест: `REMINDER_OFFSETS` из env парсится в список смещений произвольной длины; отсутствие → явная ошибка/пустой список по решению (падающий)
- [ ] 1.2 Реализовать парсинг смещений в `app/config.py` (напр. `24h,1h` → длительности + `kind`)
- [ ] 1.3 Зелёный тест 1.1

## 2. Таблица sent_reminders

- [ ] 2.1 Тест: раннер миграций создаёт `sent_reminders` (PK `(booking_id, kind)`); идемпотентно (падающий)
- [ ] 2.2 Добавить `CREATE TABLE IF NOT EXISTS sent_reminders (booking_id, kind, sent_at)` в `app/migrations.py`
- [ ] 2.3 Зелёный тест 2.1

## 3. Сервис напоминаний

- [ ] 3.1 Тест: напоминание «за X» созрело (`now ≥ start − X`, бронь будущая) → выбрано; не созрело → нет (падающий, брони/маркеры замоканы)
- [ ] 3.2 Тест: идемпотентность — при существующем маркере `(booking_id, kind)` повторной отправки нет
- [ ] 3.3 Тест: разные виды (`24h`, `1h`) независимы
- [ ] 3.4 Тест: отменённая и прошедшая бронь исключены
- [ ] 3.5 Тест: текст форматируется в TZ клиента (не UTC)
- [ ] 3.6 Тест: ошибка отправки одного получателя не роняет проход; маркер не фиксируется как отправленный (claim снимается)
- [ ] 3.7 Реализовать `app/services/reminder_service.py`: выборка кандидатов в горизонте `max(offsets)`, проверка зрелости, claim-then-send маркера, отправка через `Bot` + `formatting`, устойчивость к ошибкам
- [ ] 3.8 Зелёные тесты 3.1–3.6

## 4. Точка входа timer-функции

- [ ] 4.1 Тест: вызов `notify` обрабатывает созревшие напоминания и завершается без фоновых задач (падающий, сервис замокан)
- [ ] 4.2 Реализовать `app/notify.py`: entrypoint timer-триггера, тёплый `Bot`, один проход через `reminder_service`, `asyncio.run` для отправки; без planner/фоновых задач
- [ ] 4.3 Зелёный тест 4.1

## 5. Проверка и завершение

- [ ] 5.1 `make test` зелёный, покрытие ≥ 80%
- [ ] 5.2 `make validate` зелёный (ruff + format + mypy, без `# type: ignore`)