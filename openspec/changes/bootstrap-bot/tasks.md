## 1. Конфигурация из окружения

- [ ] 1.1 Тест: `config` читает `BOT_TOKEN`, `WEBHOOK_SECRET`, `YDB_ENDPOINT`, `YDB_DATABASE` из env; при отсутствии обязательной переменной — явная ошибка (падающий)
- [ ] 1.2 Реализовать `app/config.py`: типизированное чтение env один раз, явная ошибка при отсутствии обязательного значения, никаких захардкоженных секретов
- [ ] 1.3 Зелёный тест 1.1

## 2. Тёплый клиент YDB

- [ ] 2.1 Тест: драйвер инициализируется один раз на уровне модуля и переиспользуется между вызовами; конфиг подключения берётся из `config`, не из констант (падающий, драйвер замокан)
- [ ] 2.2 Тест: отсутствие обязательной переменной подключения YDB → явная ошибка конфигурации
- [ ] 2.3 Реализовать `app/ydb_client.py`: модульный `Driver` + пул сессий, ленивое `driver.wait(...)`, провайдер кред из env, без ручного закрытия
- [ ] 2.4 Зелёные тесты 2.1–2.2

## 3. Миграции схемы

- [ ] 3.1 Тест: раннер миграций идемпотентен — повторный запуск на «уже мигрированной» БД безопасен (падающий, сессия YDB замокана)
- [ ] 3.2 Тест: миграция не вызывается на пути обработки вебхука
- [ ] 3.3 Реализовать `app/migrations.py`: явно запускаемый идемпотентный раннер (каркас `CREATE TABLE IF NOT EXISTS`, прикладных таблиц пока нет), отдельная точка входа вне хендлера
- [ ] 3.4 Зелёные тесты 3.1–3.2

## 4. Сборка бота и хендлер /start

- [ ] 4.1 Тест: фабрика собирает `Bot`/`Dispatcher` с токеном из `config`; зарегистрирован хендлер `/start` (падающий)
- [ ] 4.2 Тест: `/start` отвечает приветственным сообщением (через мок/спай отправки)
- [ ] 4.3 Реализовать `app/bot.py`: фабрика `Bot`/`Dispatcher`, хендлер `/start`, тонкий (без доменной логики)
- [ ] 4.4 Зелёные тесты 4.1–4.2

## 5. Точка входа YC-функции (webhook)

- [ ] 5.1 Тест: валидное HTTP-событие с верным секретом → апдейт разобран и передан в Dispatcher, HTTP 200 (падающий)
- [ ] 5.2 Тест: неверный/отсутствующий секрет вебхука → апдейт не передан в Dispatcher, HTTP 403
- [ ] 5.3 Тест: некорректный JSON в теле → без необработанного исключения, апдейт не обрабатывается
- [ ] 5.4 Тест: повторный `update_id` → нет второго наблюдаемого эффекта (идемпотентность через отсутствие побочных эффектов `/start`)
- [ ] 5.5 Реализовать `app/handler.py`: разбор события YC (`body`/`headers`), проверка секрета, мост в aiogram через `asyncio.run` + `dp.feed_update`, корректные HTTP-ответы; без фоновых задач/планировщиков
- [ ] 5.6 Зелёные тесты 5.1–5.4

## 6. Проверка и завершение

- [ ] 6.1 `make test` зелёный, покрытие ≥ 80%
- [ ] 6.2 `make validate` зелёный (ruff + format + mypy, без `# type: ignore`)